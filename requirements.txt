fastapi
uvicorn[standard]
python-multipart
openai-whisper
deep-translator
gTTS
edge-tts
Pillow
paddleocr==3.7.0
# 3.3.1 has no CPython 3.11 Linux ARM64 wheel on PyPI. Docker Desktop on
# Apple Silicon builds this image as linux/aarch64, so use the newest release
# with a published wheel for that target.
paddlepaddle==3.2.1
